// let name = muskan;
// sequence of characters : strings
let name = "muskan";
// console.log(name[0]);
// name.push("a"); // This will throw an error because strings are immutable in JavaScript
// name[0] = "M"; // This will also throw an error because strings are immutable
// name = name + " Kumari";
// name = "Muskan";
let a = name.length;
// console.log(a);
console.log(name.slice(0, 3)); // "mus"
