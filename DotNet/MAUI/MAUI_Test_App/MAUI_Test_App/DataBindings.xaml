<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUI_Test_App.DataBindings"
             Title="DataBindings"
             xmlns:EmpModel="clr-namespace:MAUI_Test_App.Models"
             >
     <ContentPage.Resources>
          <EmpModel:Employee x:Key="employee" Name="Mohan" Id="2" Age="30" Salary="5000"/>
     </ContentPage.Resources>
    <VerticalStackLayout>
    <!--    <Label 
             x:Name="EmployeeLabel"
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
          <Label Text="{Binding Name,Source={StaticResource employee}}"
                 VerticalOptions="Center"
HorizontalOptions="Center"
                 /> -->
        <!--  <Label Text="{Binding Id}"
                 VerticalOptions="Center"
HorizontalOptions="Center"
                 />

          <Label Text="{Binding Name}"
                 VerticalOptions="Center"
HorizontalOptions="Center"
                 />
          <Label Text="{Binding Age}"
                 VerticalOptions="Center"
HorizontalOptions="Center"
                 />
          <Label Text="{Binding Salary}"
                 VerticalOptions="Center"
HorizontalOptions="Center"
                 /> -->
          <Label 
             x:Name="EmployeeLabel"
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
          <Label Rotation="{Binding Source={x:Reference Slider1}, Path=Value}"
                 VerticalOptions="Center"
                 HorizontalOptions="Center"
               Text="User" 
                 />
          <Slider Minimum="0" Maximum="360"
                  x:Name="Slider1"
                  
                  />
          <Button Text="Change name" Clicked="Button_Clicked"
                 HorizontalOptions="Center"
                  VerticalOptions="Center"                  />
     </VerticalStackLayout>
</ContentPage>